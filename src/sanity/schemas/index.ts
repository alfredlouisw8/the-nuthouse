import blockContent from './blockContent'
import category from './category'
import product from './product'
import thumbnail from './thumbnail'

export const schemaTypes = [product, category, thumbnail, blockContent]
